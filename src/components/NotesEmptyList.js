import React from "react";

function NotesEmptyList () {
    return(
        <section className="notes-list-empty">
            <p className="notes-list-empty">Tidak ada catatan</p>
        </section>
    );
}

export default NotesEmptyList;